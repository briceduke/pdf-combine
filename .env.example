# BLOB_READ_WRITE_TOKEN is required for browser uploads (handleUpload).
# Create a private Blob store on the Vercel project, include Development,
# then: vercel env pull .env.local --yes
BLOB_READ_WRITE_TOKEN=

# Set automatically when the Blob store is connected to the Vercel project.
# Used with VERCEL_OIDC_TOKEN for server get/put on Vercel.
BLOB_STORE_ID=

# Populated on Vercel deployments. Locally: vercel env pull .env.local --yes
# (expires ~12 hours). Workflows use this — no Trigger.dev keys.
VERCEL_OIDC_TOKEN=
